fastapi
uvicorn
sqlalchemy
python-multipart
# Assuming torch is already installed with CUDA support as per user context, 
# but listing it here for completeness.
torch 
numpy
# onnxruntime-gpu
requests
tqdm
jinja2
python-jose[cryptography]
passlib[bcrypt]
python-dotenv